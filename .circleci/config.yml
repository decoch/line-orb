version: 2.1

orbs:
  example: decoch/hello@dev:first
  line: decoch/line@dev:second

jobs:
  build:
    docker:
    - image: circleci/node
    steps:
    - run: "echo 'testing building...'"
    - example/hello:
        username: "decoch"
    - line/notify:
        message: "hello from orb"

workflows:
   main:
     jobs:
     - build
